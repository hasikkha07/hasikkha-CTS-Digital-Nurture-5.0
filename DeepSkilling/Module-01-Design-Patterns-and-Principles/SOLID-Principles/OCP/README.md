# Open/Closed Principle (OCP)

## Overview

The Open/Closed Principle states that software entities should be **open for extension but closed for modification**.

This means new functionality should be added by creating new classes instead of modifying existing code.

In this example:

- `Shape` is an abstract class.
- `Circle` and `Rectangle` extend `Shape`.
- New shapes can be added without changing existing classes.

---

## Topics Covered

- SOLID Principles
- Open/Closed Principle
- Abstraction
- Polymorphism
- Extensibility

---

## Learning Outcomes

After completing this example, I was able to:

- Understand the Open/Closed Principle.
- Extend functionality using inheritance.
- Reduce modification of existing code.
- Improve scalability and maintainability.

---

## Technologies Used

- C#
- .NET Console Application

---

## Conclusion

The Open/Closed Principle encourages developers to design software that can be extended with new features while minimizing changes to existing, tested code.